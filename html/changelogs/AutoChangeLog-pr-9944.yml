author: "AtoriBirb"
delete-after: True
changes: 
  - code_imp: "Adds two if statements to swap between mutant_variant = none and default. I would do an else but I dunno what other variables might hit it on accident. If it works, it works."
