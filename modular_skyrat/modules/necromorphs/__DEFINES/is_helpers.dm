#define ismarkermonster(O) (istype(O, /mob/living/simple_animal/hostile/necromorph))

#define ismarkerovermind(A) (istype(A, /mob/camera/marker))
#define issignal(A) istype(A, /mob/dead/observer/eye/signal)

