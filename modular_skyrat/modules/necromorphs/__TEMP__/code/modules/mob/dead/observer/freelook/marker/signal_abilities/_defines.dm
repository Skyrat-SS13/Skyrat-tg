#define TARGET_SELF	0	//The user is the initial target. Code can still search for a viable target in a radius around them
#define TARGET_PLACEMENT	1	//Use a placement click handler to position an image of the item
#define TARGET_CLICK	2	//User just clicks on a thing