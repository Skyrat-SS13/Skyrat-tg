/datum/away_controller
	/// Name of the away controller
	var/name = "Away Controller: Generic"
	/// The delay in deciseconds the SS should wait before firing
	var/ss_delay = 5 MINUTES

/datum/away_controller/proc/fire()
	return
