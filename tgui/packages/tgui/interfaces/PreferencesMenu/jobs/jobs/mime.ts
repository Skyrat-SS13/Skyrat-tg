import { Job } from "../base";
import { Service } from "../departments";

const Mime: Job = {
  name: "Mime",
  description: "...",
  department: Service,
  alt_titles: ["Mime", "Pantomimist"],
};

export default Mime;
