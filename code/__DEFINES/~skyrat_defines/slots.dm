// Starts at two to ensure lazy testers and doc unreaders do it right.
/// Makes the modular_can_equip proc not return inside can_equip.
#define CAN_EQUIP_PASS 2
/// Makes the modular_can_equip proc return TRUE inside can_equip.
#define CAN_EQUIP_TRUE 3
/// Makes the modular_can_equip proc return FALSE inside can_equip.
#define CAN_EQUIP_FALSE 4
